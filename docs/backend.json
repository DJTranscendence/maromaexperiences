
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Maroma Experiences platform.",
      "properties": {
        "id": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "accountType": { "type": "string" }
      },
      "required": ["id", "email"]
    },
    "SimulatorSession": {
      "title": "SimulatorSession",
      "type": "object",
      "description": "Records a team's game session in The Maroma Product Game.",
      "properties": {
        "id": { "type": "string" },
        "teamName": { "type": "string" },
        "emblem": { "type": "string" },
        "productType": { "type": "string" },
        "ingredients": { "type": "array", "items": { "type": "string" } },
        "config": { "type": "object", "description": "The full strategic configuration used for the simulation." },
        "aiFeedback": { "type": "object", "description": "The generated analyst report and reviews." },
        "scores": {
          "type": "object",
          "properties": {
            "earth": { "type": "number" },
            "trust": { "type": "number" },
            "resonance": { "type": "number" },
            "impact": { "type": "number" },
            "profit": { "type": "number" },
            "longevity": { "type": "number" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "SimulatorEvent": {
      "title": "SimulatorEvent",
      "type": "object",
      "description": "Real-time game events such as team joins and phase changes.",
      "properties": {
        "teamName": { "type": "string" },
        "emblem": { "type": "string" },
        "type": { "type": "string", "enum": ["join", "lab", "market"] },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "User profiles."
        }
      },
      {
        "path": "/simulator_sessions/{sessionId}",
        "definition": {
          "entityName": "SimulatorSession",
          "schema": { "$ref": "#/backend/entities/SimulatorSession" },
          "description": "Stores game results for the leaderboard."
        }
      },
      {
        "path": "/simulator_events/{eventId}",
        "definition": {
          "entityName": "SimulatorEvent",
          "schema": { "$ref": "#/backend/entities/SimulatorEvent" },
          "description": "Broadcasts real-time events to all players."
        }
      }
    ]
  }
}
